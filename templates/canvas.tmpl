<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    canvas {
      border: 2px solid rgb(120, 190, 255);
    }
  </style>
</head>
<body>
  <h2>Canvas Page</h2>
  <canvas id="svgCanvas"> </canvas>
<!-- <script>
  const svgCanvas = document.getElementById('svgCanvas');

  let currentImg;

  function loadSVG(filePath) {
    fetch(filePath)
      .then(response => response.text())
      .then(data => {
        const img = new Image();
        img.onload = () => {
          currentImg = img;
          initializeCanvas();
          drawImage(img);
        };
        img.src = `data:image/svg+xml;base64,${btoa(data)}`;
      })
      .catch(error => console.error('Error loading SVG:', error));
  }

  function initializeCanvas() {
    const canvas = document.getElementById('svgCanvas');
    const ctx = svgCanvas.getContext('2d');

    const containerHeight = window.innerHeight;
    const containerWidth = window.innerWidth;

    ctx.width = containerWidth;
    ctx.height = containerHeight;
  }

  function drawImage(img) {
    ctx.clearRect(0, 0, canvas.width, canvas.height);

    const containerWidth = img.width;
    const containerHeight = img.height;

    const scaleFactor = Math.min(containerWidth / img.width, containerHeight / img.height);
    const newWidth = img.width * scaleFactor;
    const newHeight = img.height * scaleFactor;

    const x = (containerWidth - newWidth) / 2;
    const y = (containerHeight - newHeight) / 2;

    ctx.drawImage(img, x, y, newWidth, newHeight);
  }

  window.addEventListener('resize', () => {
    const svgCanvas = document.getElementById('svgCanvas');
    svgCanvas.width = svgCanvas.clientWidth;
    svgCanvas.height = svgCanvas.clientHeight;
    if (currentImg) {
      drawImage(currentImg);
    }
  });

  window.addEventListener('DOMContentLoaded', initializeCanvas);

  const svgPath = 'resources/user-boy.svg';
  loadSVG(svgPath);
</script> -->
</body>
</html>